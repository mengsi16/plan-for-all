# 2026-04-06 Docs Hierarchy Cleanup Plan

## Goal
Align `README.md`, `README_en.md`, and `workflow-diagram.md` with the restored ownership model: `brainstorming` is the customer-facing convergence layer, while `ui-ux-pro-max` and `writing-plans` are downstream support/handoff skills.

## Smoke Check
1. Find wording that still makes `ui-ux-pro-max` look like a peer stage rather than a subordinate support step.
2. Find wording that under-describes `brainstorming` as the owner of whole-task convergence.
3. Find workflow text or diagrams that imply the wrong stage ownership.

## Remediation Steps
1. Update `README.md` wording and examples.
2. Update `README_en.md` wording and examples.
3. Update `workflow-diagram.md` to show brainstorming ownership explicitly.

## Verification
- Read back the three files in UTF-8.
- Confirm `brainstorming` is described as the owner of requirement convergence.
- Confirm `ui-ux-pro-max` is described as a subordinate refinement step.
- Confirm `writing-plans` is described as implementation handoff.
